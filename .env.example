# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=syncd

# FastAPI – generate a strong random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changethissecretkey

# CORS – comma-separated allowed origins (use * for local dev only)
CORS_ORIGINS=*
